name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Add language-specific install & lint commands below

  unit_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Add language-specific setup commands here
      # Run unit tests command below

  integration_test:
    runs-on: ubuntu-latest
    needs: unit_test
    steps:
      - uses: actions/checkout@v4
      # Add language-specific setup commands here
      # Run integration tests command below

  build:
    runs-on: ubuntu-latest
    needs: [lint, unit_test, integration_test]
    steps:
      - uses: actions/checkout@v4
      # Add language-specific build commands here

  security_scan:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Add SAST tool setup and run commands here