# Python runtime version configuration
# Updated to require Python 3.12 per upgrade specification.
# Remove/replace any previous specification of Python 3.8.

python_version: "3.12"   # Upgraded: Specifies use of Python 3.12 runtime environment

# python_version: "3.8"   # Deprecated: Support for Python 3.8 is removed in this upgrade

# No other settings specified—add configuration for particular systems (e.g., Docker, pyenv, CI) if needed.