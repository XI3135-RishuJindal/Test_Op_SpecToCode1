# Project Documentation Configuration Template
# Generated to reflect modernized stack documentation update
# All TODO markers indicate fields that must be populated with actual project values

# ============================================================
# DOCUMENTATION METADATA
# ============================================================

# Project identity — update these to match the current project state
project:
  name: "TODO: project name"
  description: "TODO: brief description of the project"
  # version: reflects the current modernized release, not the pre-migration version
  version: "TODO: current version"
  # repository: canonical source of truth for the modernized codebase
  repository: "TODO: repository URL"
  # license: confirm this has not changed during modernization
  license: "TODO: license identifier (e.g. MIT, Apache-2.0)"

# ============================================================
# STACK REFERENCES
# Updated to reflect the modernized technology stack.
# Replace all TODO values with the actual versions in use.
# Remove any entries that do not apply to this project.
# ============================================================

stack:
  # language: the primary programming language and its current stable version
  # CHANGED: updated from pre-modernization version — confirm exact version in use
  language:
    name: "TODO: language name"
    version: "TODO: current stable version"

  # runtime: the execution environment for the application
  # CHANGED: updated to reflect modernized runtime — remove if not applicable
  runtime:
    name: "TODO: runtime name"
    version: "TODO: current stable version"

  # build_tool: the tool used to compile, bundle, or package the project
  # CHANGED: update if build tooling changed during modernization
  build_tool:
    name: "TODO: build tool name"
    version: "TODO: current stable version"
    # config_file: the primary configuration file consumed by the build tool
    config_file: "TODO: e.g. package.json, pom.xml, build.gradle, Makefile"

  # frameworks: list all frameworks in use after modernization
  # CHANGED: remove deprecated frameworks; add newly adopted ones
  frameworks: []
  # Example entry format:
  # - name: "TODO: framework name"
  #   version: "TODO: current stable version"
  #   role: "TODO: e.g. web, ORM, testing, messaging"

  # dependencies: reference to the authoritative dependency manifest
  # CHANGED: point to the lock file or manifest reflecting the modernized stack
  dependencies:
    manifest_file: "TODO: e.g. package-lock.json, requirements.txt, go.sum, Gemfile.lock"
    # dependency_docs: path or URL to human-readable dependency documentation
    dependency_docs: "TODO: path or URL, or remove if not maintained separately"

# ============================================================
# ENVIRONMENT CONFIGURATION
# Documents the environment variables and configuration schema
# required to run the modernized stack.
# ============================================================

environment:
  # setup_guide: path to the environment setup documentation artifact
  # CHANGED: update to point to the revised setup guide for the modernized stack
  setup_guide: "TODO: e.g. docs/setup.md"

  # required_variables: environment variables that must be set for the application to run
  # CHANGED: audit and update this list — remove variables tied to deprecated components
  required_variables: []
  # Example entry format:
  # - name: "TODO: ENV_VAR_NAME"
  #   description: "TODO: what this variable controls"
  #   default: "TODO: default value, or null if required with no default"

  # optional_variables: environment variables that tune behavior but have safe defaults
  optional_variables: []
  # Example entry format:
  # - name: "TODO: ENV_VAR_NAME"
  #   description: "TODO: what this variable controls"
  #   default: "TODO: default value"

# ============================================================
# DOCUMENTATION ARTIFACTS
# Inventory of all documentation files that must be kept
# consistent with the modernized stack.
# ============================================================

documentation:
  artifacts:
    # readme: primary entry point for contributors and operators
    # CHANGED: README must be updated to remove pre-modernization stack references
    - id: readme
      path: "README.md"
      status: "TODO: needs-update | up-to-date"
      description: "Project overview, quickstart, and stack summary"

    # architecture: describes system design and component relationships
    # CHANGED: update to reflect any architectural changes introduced by modernization
    - id: architecture
      path: "TODO: e.g. docs/architecture.md"
      status: "TODO: needs-update | up-to-date"
      description: "Architecture overview and component diagram"

    # setup: step-by-step environment and build setup instructions
    # CHANGED: must reflect new runtime, build tool, and dependency installation steps
    - id: setup
      path: "TODO: e.g. docs/setup.md"
      status: "TODO: needs-update | up-to-date"
      description: "Developer environment setup guide"

    # contributing: guidelines for contributors
    # CHANGED: update workflow steps, tooling commands, and version requirements
    - id: contributing
      path: "TODO: e.g. CONTRIBUTING.md"
      status: "TODO: needs-update | up-to-date"
      description: "Contribution guidelines and development workflow"

    # runbook: operational procedures for running and maintaining the system
    # CHANGED: update commands, configuration paths, and runtime references
    - id: runbook
      path: "TODO: e.g. docs/runbook.md"
      status: "TODO: needs-update | up-to-date | not-applicable"
      description: "Operational runbook for deployment and incident response"

    # changelog: record of changes between versions
    # CHANGED: ensure modernization changes are recorded here
    - id: changelog
      path: "TODO: e.g. CHANGELOG.md"
      status: "TODO: needs-update | up-to-date"
      description: "Version history and migration notes"

    # api_reference: API contracts and interface documentation
    # CHANGED: update if API surface changed during modernization
    - id: api_reference
      path: "TODO: e.g. docs/api.md or link to generated docs"
      status: "TODO: needs-update | up-to-date | not-applicable"
      description: "API reference documentation"

  # deprecated_artifacts: documentation files that described the pre-modernization stack
  # and should be archived or removed to prevent confusion
  # CHANGED: populate this list with any docs that are no longer valid
  deprecated_artifacts: []
  # Example entry format:
  # - path: "TODO: path to deprecated doc"
  #   reason: "TODO: why it is deprecated (e.g. described removed framework X)"
  #   action: "TODO: archive | delete | redirect"

# ============================================================
# OPEN QUESTIONS
# Items that must be resolved before documentation can be
# considered complete and accurate.
# ============================================================

open_questions:
  - id: oq-1
    question: "What are the exact language and runtime versions in use after modernization?"
    owner: "TODO"
    status: "open"

  - id: oq-2
    question: "Which documentation artifacts currently exist and which contain outdated stack references?"
    owner: "TODO"
    status: "open"

  - id: oq-3
    question: "Were any frameworks added or removed during modernization that are not yet reflected in docs?"
    owner: "TODO"
    status: "open"

  - id: oq-4
    question: "Are there compliance or audit requirements that mandate documentation accuracy by a specific date?"
    owner: "TODO"
    status: "open"

  - id: oq-5
    question: "Is there a CI/CD step that validates documentation consistency with the actual stack?"
    owner: "TODO"
    status: "open"