# Example entries for enabling automated test coverage tooling

# Enable test coverage reporting (set to 'true' to activate)
TEST_COVERAGE_ENABLED=true

# Directory for storing test coverage reports
TEST_COVERAGE_REPORT_DIR=coverage/

# Specify minimum test coverage percentage (fail build if below this value)
TEST_COVERAGE_THRESHOLD=80

# Environment name for running tests
TEST_ENVIRONMENT=testing