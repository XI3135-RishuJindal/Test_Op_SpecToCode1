# No base configuration file was provided in "Base Source Code", and the upgrade context/spec does not identify:
# - the language/runtime/build tool
# - the framework(s)
# - any concrete dependencies, versions, or upgraded components
# - any configuration schema/keys that changed due to the upgrade
#
# Per instructions, we must not invent key names or default values. Therefore, a usable configuration template
# cannot be safely generated without the missing details.
#
# Provide one of the following so this can be updated in-place or generated correctly:
# 1) The current config file(s) from the repo (e.g., package.json/pyproject.toml/go.mod/pom.xml/build.gradle, plus app config like .env, application.yml, etc.)
# 2) The exact vulnerable packages + target patched versions (and the framework/runtime), and the config format to output.
#
# Once provided, this file will be replaced with the complete updated configuration, with inline comments for each new/changed/deprecated key.