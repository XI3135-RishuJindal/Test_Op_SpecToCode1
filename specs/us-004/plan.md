To implement the authorization code exchange at the IdP token endpoint, we will extend the functionality in the `AuthController` class located in `Controllers/AuthController.cs`. We will introduce a method to handle the internal logic of sending an HTTPS POST request to the IdP token endpoint using the .NET `HttpClient`. This method will transform the received tokens into a format useful for downstream processes and omit log statements that could reveal sensitive tokens. A new configuration section for client credentials and redirect URIs should be added to `appsettings.json`. Additionally, ensure new test cases in `Tests/Controllers/AuthControllerTests.cs` to evaluate correct token exchange and error handling. The integration must pass existing and new unit tests to confirm compliance with functional and security standards.